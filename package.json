{
  "name": "gehenna",
  "version": "1.0.0",
  "description": "- `Array.find()`\r - `Array.map()`\r - `Array.includes()`\r - `Array.forEach()`\r - `Array.flat()`\r - `Array.filter()`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Thomas Scharstein II <tom@inventsable.cc>",
  "license": "ISC",
  "directories": {
    "lib": "lib"
  }
}
